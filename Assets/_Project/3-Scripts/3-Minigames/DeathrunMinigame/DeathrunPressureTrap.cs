using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeathrunPressureTrap : MonoBehaviour
{
    public float majorityVote;
    public float currentVote;

    public GameObject triggerZone;
    
    // when majority is reached trap is activated
    // votes consist of dont activate and activate votes which delay or actiavte the trap respectively
    // making checkpoints to identify which trap the player is on
    // trap activates in chat and then does next voting period after a delay


    public void ActivateTrap()
    {
        // if player is in zone, then player dead
        if ()
        {

        }
    }
}
